{
  "name": "asafarim-terminal",
  "version": "1.1.0",
  "description": "A simple terminal interface using Node.js",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "clean": "rm -rf node_modules .pnpm pnpm-lock.yaml",
    "reinstall": "echo \" 📦 Clearing cache and installing dependencies...\" && pnpm store prune && pnpm clean && pnpm install && pnpm approve-builds",
    "rebuild": "node-gyp rebuild"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-pty": "^1.1.0-beta34",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "dotenv": "^16.5.0"
  }
}
